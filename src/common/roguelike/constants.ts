/** Base starting TR for a roguelike solo run before upgrade/ascension bonuses. */
export const ROGUELIKE_BASE_TR = 5;

/** Base starting project cards to choose from before upgrade/ascension bonuses. */
export const ROGUELIKE_BASE_STARTING_CARDS = 5;

/** Max extra prelude options from the upgrade tree (beyond prelude slots). */
export const ROGUELIKE_MAX_UPGRADE_PRELUDE_CHOICES = 6;
